#include <stdio.h>

void fibonacci(int n) {
    if (){
        
    }
    int serie = (n - 1) + n;
    printf("%d", serie);
    fibonacci(serie);
}

int main(){
    int max = 10;
    fibonacci(0, max)
}